name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx prisma generate"
dev = "npx prisma migrate deploy && npx concurrently -k -s first \"npm exec remix vite:dev\" \"npm run worker:generation\""
